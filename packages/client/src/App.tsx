// This file is intentionally empty.
// The application root is now managed by the TanStack Router in router.tsx.
// The root route layout (div.app + TipJar) lives in router.tsx.
// The useServerMessages hook is initialised inside HomeScreen / GameScreen as needed.
export {};
